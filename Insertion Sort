void insertionSort( int v[], int n ) {
    for ( int i = 1 ; i < n ; i++ ) {
        int x = v[i], j = i - 1;
        while ( j >= 0 && v[j] > x ) {
            v[j + 1] = v[j];
            j--;
        }
        v[j + 1] = x;
    }
}
